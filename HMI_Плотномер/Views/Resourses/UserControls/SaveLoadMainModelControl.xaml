<UserControl x:Class="IDensity.Views.Resourses.UserControls.SaveLoadMainModelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:IDensity.ViewModels"
             xmlns:local="clr-namespace:IDensity.Core.Views.Resourses.UserControls"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:CommonSettingsVm}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListBox HorizontalContentAlignment="Stretch">
            <DockPanel>
                <TextBlock Text="Сохранить"/>
                <Button HorizontalAlignment="Right" Width="100" Command="{Binding WriteSettingsToFile}">--></Button>
            </DockPanel>
        </ListBox>    
    </Grid>
</UserControl>
