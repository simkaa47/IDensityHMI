<Page x:Class="IDensity.Views.CheckMaster.StartMasterPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:vm="clr-namespace:IDensity.ViewModels.MasrerSettings"
      xmlns:local="clr-namespace:IDensity.Views.CheckMaster"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="StartMasterPage">

    <Grid>
        <TextBlock Text="Добро пожаловать в мастер проверки прибора. Для того, чтобы начать проверку, нажмите кнопку 'Далее', иначе нажмите кнопку 'Отмена'"
                   TextWrapping="Wrap"
                   FontSize="20"
                   Margin="20"/>
    </Grid>
</Page>
